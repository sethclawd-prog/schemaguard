# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({ command: 'node', args: ['mcp-server.js'] })
  exampleConfig: {}

# Override entry point for Smithery build/scan
build:
  entry: smithery-entry.mjs
